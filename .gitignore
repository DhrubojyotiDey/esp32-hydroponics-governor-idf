# ── PlatformIO build outputs ─────────────────────────────────
.pio/

# ── IDF managed components (auto-downloaded on first build) ──
# Re-fetched via src/idf_component.yml — do not commit
managed_components/
dependencies.lock

# ── ESP-IDF generated config (DO NOT commit — rebuild from sdkconfig.defaults)
sdkconfig

# ── VS Code workspace settings (local only) ─────────────────
.vscode/

# ── OS generated files ───────────────────────────────────────
.DS_Store
Thumbs.db

# ── Staging / scratch folder (original flat files) ──────────
files/

